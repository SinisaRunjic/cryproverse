import { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    //seed token
    colorPrimary: "#454554",
    borderRadius: 2,
  },
  //style of components
  components: {
    // style of layout
    Layout: {
      // style of header
      headerBg: '#f5f5f5'
    }
  }
};

export default theme;
